if [ -d "$HOME/bin" ] ; then
    PATH="$HOME/bin:$PATH"
fi

export EDITOR="emacs -nw --no-desktop -Q"

alias l="ls -CFBh --ignore='#*#' --ignore='*~' --color='always'"
alias la="l -A"
alias ll="la -l"

source ~/.git-completion.bash

[[ $- == *i* ]]   && . ~/.git-prompt/git-prompt.sh